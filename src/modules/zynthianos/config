###############################################################################
# All this module config settings must start with ZYNTHIANOS_

[ -n "$BASE_ZIP_IMG" ] || BASE_ZIP_IMG=`ls -t $BASE_IMAGE_PATH/*zynthianos*.zip | head -n 1`
